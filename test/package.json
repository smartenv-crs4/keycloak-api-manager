{
  "name": "keycloak-api-manager-tests",
  "version": "1.0.0",
  "description": "Test suite for keycloak-api-manager",
  "private": true,
  "scripts": {
    "test": "NODE_ENV=test mocha",
    "test:watch": "NODE_ENV=test mocha --watch --watch-extensions js"
  },
  "dependencies": {
    "keycloak-api-manager": "file:..",
    "chai": "^4.3.10",
    "dockerode": "^4.0.2",
    "mocha": "^10.2.0",
    "propertiesmanager": "^4.1.0"
  }
}
